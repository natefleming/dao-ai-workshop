[project]
name = "lab-25-agent-as-tool"
version = "0.1.0"
description = "Lab 25 -- demonstrate the first-class type: app and type: serving_endpoint tool kinds (dao-ai 0.1.98+)"
requires-python = ">=3.10"
dependencies = [
    "dao-ai>=0.1.99",
    "openai>=1.40",
]
