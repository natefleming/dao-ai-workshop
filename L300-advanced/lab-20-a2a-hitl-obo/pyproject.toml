[project]
name = "lab-20-a2a-hitl-obo"
version = "0.0.1"
description = "Lab 20 — A2A + HITL + OBO — auto-derived OAuth2 scheme, resume via DataPart decisions"
requires-python = ">=3.11"
dependencies = [
    "dao-ai>=0.1.80",
    "a2a-sdk>=0.3.0,<1.0.0",
]
