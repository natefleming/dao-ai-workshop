# yaml-language-server: $schema=../../../dao-ai/schemas/model_config_schema.json
#
# Lab 7 - Persistent Memory + Summarization (saas_helpdesk)
# ---------------------------------------------------------
# A SaaS support assistant with two layers of memory plus chat-history
# summarization:
#
#   - memory.checkpointer  -- short-term, scoped to thread_id (thread state survives restarts)
#   - memory.store         -- long-term, scoped to user_id (facts persist across threads)
#   - memory.extraction    -- background pipeline that writes facts (name, recurring issues,
#                             stated preferences) into the store
#   - app.chat_history     -- when the message history exceeds the token budget, older turns
#                             are compressed into a summary message by a small/fast LLM
#
# All persistence is backed by Databricks Lakebase (managed Postgres).
#
# Builds on:
#   - Lab 1: baseline structure (parameters / resources.llms / agents / app).
#   - Lab 5: switched to the saas_helpdesk use case.
#
# What's new vs prior labs (every block below introduces something):
#   - parameters: secret-scope-wired client_id/client_secret pattern
#     (later labs reuse this when an SP needs explicit credentials).
#   - variables:                      composite variable with multi-source fallback
#     (used by checkpointer/store secrets).
#   - resources.databases:            Lakebase autoscaling project as a resource.
#   - top-level memory:               checkpointer (thread state) + store
#                                     (cross-thread facts) + extraction (LLM
#                                     pipeline that writes facts to the store).
#   - app.orchestration.memory:       wires the memory section into runtime.
#   - app.chat_history:               auto-summarization when token budget exceeded.

parameters:
  username:
    description: Per-student username for unique deployment names. Auto-set by the notebook.
    # No default => required.
  lakebase_name:
    description: Lakebase instance name.
    default: retail-consumer-goods
  lakebase_project:
    description: Lakebase project name.
    default: retail-consumer-goods
  llm_endpoint:
    description: Primary Databricks LLM serving endpoint name.
    default: databricks-claude-sonnet-4-5
  fast_llm_endpoint:
    description: Faster LLM endpoint used for memory queries.
    default: databricks-gpt-oss-120b
  summarization_llm_endpoint:
    description: Smaller, faster LLM used to compress older turns into a summary message.
    default: databricks-gpt-5-nano
  max_tokens_before_summary:
    description: Trigger chat-history summarization when history exceeds this many tokens.
    default: "6000"
  max_tokens_after_summary:
    description: Target token budget kept verbatim after summarization.
    default: "2048"
  # Service-principal credentials for Lakebase. The notebook runs as
  # the calling user (PAT/ambient auth) and works without these. The
  # deployed Databricks App runs as a service principal that needs
  # explicit Lakebase access. Lakebase isn't an App "resource" -- it
  # uses the `postgres` API scope and identity auth -- so we provide
  # client_id/client_secret directly on the database.
  sp_secret_scope:
    description: Databricks secret scope holding the Lakebase SP credentials.
    default: dao_ai_workshop
  sp_client_id_secret:
    description: Secret key within the scope for the SP client_id.
    default: DAO_AI_SP_CLIENT_ID
  sp_client_secret_secret:
    description: Secret key within the scope for the SP client_secret.
    default: DAO_AI_SP_CLIENT_SECRET

variables:
  # Composite variables try sources in order, return the first non-None.
  # Secret scope wins; falls back to a process env var so notebook runs
  # can supply ambient credentials without Databricks secret access.
  client_id: &client_id
    options:
      - scope: ${var.sp_secret_scope}
        secret: ${var.sp_client_id_secret}
      - env: DAO_AI_SP_CLIENT_ID

  client_secret: &client_secret
    options:
      - scope: ${var.sp_secret_scope}
        secret: ${var.sp_client_secret_secret}
      - env: DAO_AI_SP_CLIENT_SECRET

resources:
  llms:
    default_llm: &default_llm
      name: ${var.llm_endpoint}
      temperature: 0.4
      max_tokens: 4096

    fast_llm: &fast_llm
      name: ${var.fast_llm_endpoint}
      temperature: 0.1
      max_tokens: 2048

    summarization_llm: &summarization_llm
      name: ${var.summarization_llm_endpoint}
      temperature: 0.1
      max_tokens: 512

  databases:
    workshop_db: &workshop_db
      name: ${var.lakebase_name}
      project: ${var.lakebase_project}
      description: "Lakebase used by the workshop for persistent memory."
      # SP auth for the deployed app. Notebook runs ignore these
      # (ambient auth wins); the deployed app uses them.
      client_id: *client_id
      client_secret: *client_secret
      on_behalf_of_user: false

agents:
  support_agent: &support_agent
    name: support_agent
    description: A SaaS support assistant with cross-session memory + auto-summarization.
    model: *default_llm
    prompt: |
      You are a tier-1 support assistant with persistent memory.
      When a returning customer speaks to you, reference their prior
      tickets and stated context naturally. Don't make them repeat
      themselves.

      If you see a conversation summary at the start of the message
      history, treat it as authoritative context for what was discussed
      earlier. Reference it naturally rather than acting like the
      conversation just started.

memory: &memory
  checkpointer:
    name: support_checkpointer
    database: *workshop_db

  store:
    name: support_store
    database: *workshop_db
    namespace: "{user_id}"

  extraction:
    schemas: [user_profile, preference]
    instructions: |
      Extract the customer's name, the products they use, recurring
      issues, and any stated preferences (channel, urgency, contact times).
    auto_inject: true
    auto_inject_limit: 5
    background_extraction: false
    extraction_model: *default_llm
    query_model: *fast_llm

app:
  name: saas-helpdesk-${var.username}
  description: "Lab 7: persistent memory + chat-history summarization"
  log_level: INFO
  deployment_target: apps

  agents:
    - *support_agent

  # Single-agent app: orchestration only wires the memory section
  # into runtime. No swarm/supervisor router needed -- the support_agent
  # is reached directly so the user-supplied `configurable` (thread_id,
  # user_id, ...) flows straight into the middleware chain.
  orchestration:
    memory: *memory

  # Once the chat history exceeds max_tokens_before_summary, dao-ai
  # uses summarization_llm to compress older turns into a single
  # summary message. Recent turns are kept verbatim.
  chat_history:
    model: *summarization_llm
    max_tokens: ${var.max_tokens_after_summary}
    max_tokens_before_summary: ${var.max_tokens_before_summary}
